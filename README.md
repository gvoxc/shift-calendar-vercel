# 白夜班计算器

一个可直接部署到 Vercel 的最小 Next.js 项目。

## 本地运行

```bash
npm install
npm run dev
```

打开 `http://localhost:3000`

## 部署到 Vercel

1. 把本项目上传到 GitHub
2. 在 Vercel 里导入这个仓库
3. 直接 Deploy 即可

## 功能

- 选择某一天作为白班基准日
- 按 `白班 -> 夜班 -> 休息 -> 休息` 自动循环
- 显示今天的值班状态
- 用月历视图展示每天的班次
